# 前端 Frontend

TanStack Router 页面与 React 组件。

| 路径 | 说明 |
|------|------|
| `routes/` | 页面路由（首页、`/whois/$query`） |
| `components/` | UI 组件（首页、主题切换） |
| `lib/http.ts` | 请求/响应工具（前后端共用） |
| `styles.css` | 全局样式 |
| `router.tsx` | 路由入口 |

本地开发：`pnpm dev` → http://localhost:3410
